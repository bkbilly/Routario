# Workspace Guidelines & Rules

## Git & Version Control
- **Never create git commits without explicit user request or approval.**
- Do not proactively run `git commit` or `git add` unless the user explicitly asks to commit changes.
- Always present the summary of changes and ask for confirmation before committing.
